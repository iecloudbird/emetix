# 2. System Architecture

> **Complete Technical Architecture: Backend to Frontend**

---

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              EMETIX PLATFORM ARCHITECTURE                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                         FRONTEND (React/Next.js)                         │    │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐  │    │
│  │  │ Dashboard │ │ Watchlist │ │  Stock    │ │  Compare  │ │  Sectors  │  │    │
│  │  │   Page    │ │   Page    │ │  Detail   │ │   View    │ │   View    │  │    │
│  │  └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └─────┬─────┘  │    │
│  │        └─────────────┴───────────┬─┴─────────────┴─────────────┘        │    │
│  │                                  │                                       │    │
│  │  ┌───────────────────────────────▼───────────────────────────────────┐  │    │
│  │  │                    React Query / SWR (Data Fetching)              │  │    │
│  │  └───────────────────────────────┬───────────────────────────────────┘  │    │
│  └──────────────────────────────────┼──────────────────────────────────────┘    │
│                                     │ HTTPS/REST                                 │
│  ═══════════════════════════════════╪═══════════════════════════════════════    │
│                                     │                                            │
│  ┌──────────────────────────────────▼──────────────────────────────────────┐    │
│  │                         BACKEND (FastAPI)                                │    │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │    │
│  │  │                    API Layer (src/api/)                            │ │    │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │ │    │
│  │  │  │/watchlist│ │ /stock/  │ │ /compare │ │ /sectors │ │ /charts/ │ │ │    │
│  │  │  │          │ │ {ticker} │ │          │ │          │ │ {ticker} │ │ │    │
│  │  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ │ │    │
│  │  └───────┼────────────┼───────────┼────────────┼────────────┼────────┘ │    │
│  │          └────────────┴───────────┼────────────┴────────────┘          │    │
│  │                                   │                                     │    │
│  │  ┌────────────────────────────────▼────────────────────────────────┐   │    │
│  │  │                 Analysis Layer (src/analysis/)                   │   │    │
│  │  │  ┌─────────────────────────────────────────────────────────────┐│   │    │
│  │  │  │                    StockScreener                            ││   │    │
│  │  │  │  • Dynamic sector benchmarks                                ││   │    │
│  │  │  │  • Valuation scoring (0-100)                                ││   │    │
│  │  │  │  • Buy/Hold/Sell recommendations                            ││   │    │
│  │  │  │  • Justification generation                                 ││   │    │
│  │  │  └─────────────────────────────────────────────────────────────┘│   │    │
│  │  └─────────────────────────────────────────────────────────────────┘   │    │
│  │                                   │                                     │    │
│  │  ┌───────────────┬────────────────┼───────────────┬───────────────┐    │    │
│  │  │               │                │               │               │    │    │
│  │  ▼               ▼                ▼               ▼               ▼    │    │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────────────┐ ┌─────────┐ ┌─────────┐   │    │
│  │ │LSTM-DCF │ │  GARP   │ │   LangChain     │ │ Yahoo   │ │  Alpha  │   │    │
│  │ │ Model   │ │ Scorer  │ │   Agents        │ │ Finance │ │ Vantage │   │    │
│  │ │ (.pth)  │ │ (Pure)  │ │ (Google Gemini) │ │  API    │ │   API   │   │    │
│  │ └─────────┘ └─────────┘ └─────────────────┘ └─────────┘ └─────────┘   │    │
│  │      │           │              │                  │          │        │    │
│  │      └───────────┴──────────────┼──────────────────┴──────────┘        │    │
│  │                                 │                                       │    │
│  │  ┌──────────────────────────────▼──────────────────────────────────┐   │    │
│  │  │                    Data Layer (data/)                            │   │    │
│  │  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────────┐ │   │    │
│  │  │  │    raw/        │  │   processed/   │  │     cache/         │ │   │    │
│  │  │  │ (API responses)│  │ (training data)│  │ (screener cache)   │ │   │    │
│  │  │  └────────────────┘  └────────────────┘  └────────────────────┘ │   │    │
│  │  └─────────────────────────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## � Microservices Architecture (Jan 2025)

The system is designed for **graceful degradation** - each component can fail independently:

```
┌─────────────────────────────────────────────────────────────────┐
│                     MICROSERVICES ARCHITECTURE                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐            │
│  │   FRONTEND  │   │     API     │   │  ML LAYER   │            │
│  │  (Next.js)  │──▶│  (FastAPI)  │──▶│             │            │
│  │             │   │             │   │             │            │
│  └─────────────┘   └─────────────┘   └─────────────┘            │
│                                              │                   │
│                    ┌─────────────────────────┴──────┐            │
│                    │                                │            │
│           ┌────────▼────────┐            ┌─────────▼─────────┐  │
│           │   LSTM-DCF      │            │  GARP Calculator  │  │
│           │   (PyTorch)     │            │  (Pure Python)    │  │
│           │                 │            │                   │  │
│           │  models/*.pth   │            │  No dependencies  │  │
│           └─────────────────┘            └───────────────────┘  │
│                    │                                │            │
│                    └────────────────┬───────────────┘            │
│                                     │                            │
│                           ┌─────────▼─────────┐                  │
│                           │  Consensus Scorer │                  │
│                           │  (Weighted Avg)   │                  │
│                           └───────────────────┘                  │
│                                                                  │
│  BENEFITS:                                                       │
│  ✓ LSTM failure → GARP + Risk still work (graceful degradation) │
│  ✓ No RF dependency → Simpler deployment                        │
│  ✓ Frontend agnostic → Just consumes API responses              │
│  ✓ Each component testable independently                        │
└─────────────────────────────────────────────────────────────────┘
```

| Scenario           | Result                                          |
| ------------------ | ----------------------------------------------- |
| LSTM fails to load | GARP + Risk still provide scores (50% capacity) |
| GARP data missing  | Falls back to trailing P/E or neutral (0.5)     |
| API down           | Frontend shows cached data or error state       |
| Frontend down      | API still serves other clients                  |

---

## �📊 Data Flow Architecture

### Offline: Data Collection & Training

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         OFFLINE: DATA COLLECTION & TRAINING                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐                   │
│  │Alpha Vantage │    │   Finnhub    │    │Yahoo Finance │                   │
│  │(Financials)  │    │ (Fallback)   │    │ (Timeseries) │                   │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘                   │
│         │                   │                   │                            │
│         ▼                   ▼                   ▼                            │
│  ┌────────────────────────────────────────────────────────┐                 │
│  │           data/raw/ (CACHED STORAGE)                   │                 │
│  │  ├── financial_statements/  (quarterly financials)     │                 │
│  │  ├── timeseries/           (daily price data)          │                 │
│  │  └── finnhub_financials/   (backup source)             │                 │
│  └────────────────────────────────────────────────────────┘                 │
│                           │                                                  │
│                           ▼                                                  │
│  ┌────────────────────────────────────────────────────────┐                 │
│  │         data/processed/ (TRAINING DATA)                │                 │
│  │  ├── lstm_dcf_training/  → LSTM model training         │                 │
│  │  ├── rf_training_data.csv → RF model training          │                 │
│  │  └── backtesting/        → Model validation            │                 │
│  └────────────────────────────────────────────────────────┘                 │
│                           │                                                  │
│                           ▼                                                  │
│  ┌────────────────────────────────────────────────────────┐                 │
│  │              models/ (TRAINED MODELS)                  │                 │
│  │  ├── lstm_dcf_enhanced.pth  (Growth forecasting)       │                 │
│  │  └── lstm_dcf_scaler.pkl    (Feature normalization)    │                 │
│  │                                                        │                 │
│  │  NOTE: Consensus = LSTM 50% + GARP 25% + Risk 25%      │                 │
│  └────────────────────────────────────────────────────────┘                 │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Online: Screening & Analysis

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    ONLINE: SCREENING & ANALYSIS                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  User Request (Frontend / API)                                               │
│         │                                                                    │
│         ▼                                                                    │
│  ┌────────────────────────────────────────────────────────┐                 │
│  │        CachedDataFetcher                               │                 │
│  │   1. Check cache (data/cache/)                         │                 │
│  │   2. If stale/missing → fetch from Yahoo Finance       │                 │
│  │   3. Update cache (6-hour expiry)                      │                 │
│  └────────────────────────────────────────────────────────┘                 │
│         │                                                                    │
│         ▼                                                                    │
│  ┌────────────────────────────────────────────────────────────────┐         │
│  │              VALUATION ENGINE (Revised Jan 2025)                │         │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │         │
│  │  │ LSTM-DCF     │ │ GARP Score   │ │ Risk Score   │            │         │
│  │  │ (Fair Value) │ │ (Fwd P/E+PEG)│ │ (Beta+Vol)   │            │         │
│  │  │    50%       │ │     25%      │ │     25%      │            │         │
│  │  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘            │         │
│  │         │                │                │                     │         │
│  │         ▼                ▼                ▼                     │         │
│  │  ┌──────────────────────────────────────────────┐              │         │
│  │  │          Consensus Scorer                     │              │         │
│  │  │  Weighted voting + MoS penalty → Final score  │              │         │
│  │  └──────────────────────────────────────────────┘              │         │
│  │                        │                                        │         │
│  │                        ▼                                        │         │
│  │  ┌──────────────────────────────────────────────┐              │         │
│  │  │      Dynamic Sector Benchmarks               │              │         │
│  │  │  Calculate median P/E, P/B from scanned data │              │         │
│  │  └──────────────────────────────────────────────┘              │         │
│  └────────────────────────────────────────────────────────────────┘         │
│                           │                                                  │
│                           ▼                                                  │
│  ┌────────────────────────────────────────────────────────┐                 │
│  │                   API Response                          │                 │
│  │   {                                                     │                 │
│  │     ticker, fair_value, valuation_score,               │                 │
│  │     recommendation, justification, sector_benchmarks   │                 │
│  │   }                                                     │                 │
│  └────────────────────────────────────────────────────────┘                 │
│                           │                                                  │
│                           ▼                                                  │
│                      Frontend Display                                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Personal Risk Capacity Flow (Thesis Innovation)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    PERSONAL RISK CAPACITY FRAMEWORK                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                    FRONTEND (Next.js)                                │    │
│  │  ┌───────────────────────────────────────────────────────────────┐  │    │
│  │  │  Risk Questionnaire (7 Questions)                             │  │    │
│  │  │  ├── Experience Level                                         │  │    │
│  │  │  ├── Investment Horizon                                       │  │    │
│  │  │  ├── Emergency Fund                                           │  │    │
│  │  │  ├── Monthly Investment %                                     │  │    │
│  │  │  ├── Max Tolerable Loss %                                     │  │    │
│  │  │  ├── Panic Sell Response                                      │  │    │
│  │  │  └── Volatility Comfort (1-5)                                 │  │    │
│  │  └─────────────────────────────┬─────────────────────────────────┘  │    │
│  │                                │ POST /api/risk-profile/assess       │    │
│  │                                ▼                                     │    │
│  │  ┌───────────────────────────────────────────────────────────────┐  │    │
│  │  │  Profile Results                                              │  │    │
│  │  │  ├── Risk Capacity Score (0-100)                              │  │    │
│  │  │  ├── Risk Tolerance Score (0-100)                             │  │    │
│  │  │  ├── Emotional Buffer (1.0-2.0x)                              │  │    │
│  │  │  ├── Suitable Beta Range (e.g., 0.5-1.2)                      │  │    │
│  │  │  └── Adjusted MoS Threshold (e.g., 15%)                       │  │    │
│  │  └─────────────────────────────┬─────────────────────────────────┘  │    │
│  │                                │ SAVE TO localStorage                │    │
│  │                                ▼                                     │    │
│  │  ┌───────────────────────────────────────────────────────────────┐  │    │
│  │  │  localStorage (Client-Side Storage)                           │  │    │
│  │  │  ├── emetix_profile_id: "uuid-xxx"                            │  │    │
│  │  │  └── emetix_risk_profile: { full profile JSON }               │  │    │
│  │  └───────────────────────────────────────────────────────────────┘  │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  ═══════════════════════════════════════════════════════════════════════    │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                         BACKEND (FastAPI)                             │   │
│  │                                                                       │   │
│  │  ┌────────────────────────────────────────────────────────────────┐  │   │
│  │  │  PersonalRiskCapacityService (src/analysis/)                   │  │   │
│  │  │  ├── assess_risk_profile()  → Calculate capacity + tolerance  │  │   │
│  │  │  ├── get_emotional_buffer() → Experience-based multiplier     │  │   │
│  │  │  ├── get_suitable_beta_range() → Min/max beta for profile     │  │   │
│  │  │  ├── calculate_position_sizing() → Kelly-inspired sizing      │  │   │
│  │  │  ├── filter_suitable_stocks() → Match stocks to profile       │  │   │
│  │  │  └── annotate_stocks_with_suitability() → Add ratings         │  │   │
│  │  └────────────────────────────────────────────────────────────────┘  │   │
│  │                                                                       │   │
│  │  ┌────────────────────────────────────────────────────────────────┐  │   │
│  │  │  API Routes (src/api/routes/risk_profile.py)                   │  │   │
│  │  │  ├── POST /api/risk-profile/assess                             │  │   │
│  │  │  ├── GET  /api/risk-profile/profile/{id}                       │  │   │
│  │  │  ├── POST /api/risk-profile/position-sizing                    │  │   │
│  │  │  ├── GET  /api/risk-profile/suitable-stocks                    │  │   │
│  │  │  └── GET  /api/risk-profile/methodology                        │  │   │
│  │  └────────────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  THESIS INNOVATION:                                                          │
│  ✓ Same stock, different risk for different investors                       │
│  ✓ Emotional buffer prevents overexposure during panic                      │
│  ✓ Position sizing adapts to individual capacity                            │
│  ✓ No server storage needed - privacy-preserving design                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3-Stage Quality Growth Pipeline (Phase 3)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    3-STAGE QUALITY GROWTH PIPELINE                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  STAGE 0: UNIVERSE (Monthly Refresh)                                │    │
│  │  • Source: NASDAQ FTP (~5,700 tickers)                              │    │
│  │  • Filters: Market Cap > $1B, Volume > 500K, Common Stock           │    │
│  │  • Output: ~2,000 tradeable US stocks                               │    │
│  │  • Storage: MongoDB `universe.us_stocks`                            │    │
│  └─────────────────────────────────┬───────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  STAGE 1: ATTENTION TRIGGERS (Weekly Scan)                          │    │
│  │                                                                     │    │
│  │  Trigger A: 52-Week Drop                                            │    │
│  │  └── Price ≤ 40% of 52W high AND FCF+ AND D/E < 2.0                 │    │
│  │                                                                     │    │
│  │  Trigger B: Quality Growth Gate (4 paths)                           │    │
│  │  └── Revenue Growth + FCF ROIC combinations                         │    │
│  │                                                                     │    │
│  │  Trigger C: Deep Value                                              │    │
│  │  └── LSTM MoS > 40% OR FCF Yield > 10%                              │    │
│  │                                                                     │    │
│  │  • Output: ~200-400 stocks with ALL triggers tracked                │    │
│  │  • Storage: MongoDB `pipeline.attention_stocks`                     │    │
│  └─────────────────────────────────┬───────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  STAGE 2: QUALITY VALIDATION (Daily Update)                         │    │
│  │                                                                     │    │
│  │  Pillar Scoring (each 0-100, 25% weight):                           │    │
│  │  ├── VALUE: MoS (LSTM), P/E vs sector, Forward P/E, EV/EBITDA       │    │
│  │  ├── QUALITY: FCF ROIC, ROE, Profit Margin, D/E                     │    │
│  │  ├── GROWTH: Revenue, Earnings, LSTM Predicted, PEG                 │    │
│  │  └── SAFETY: Beta, Volatility, Current Ratio, MoS Buffer            │    │
│  │                                                                     │    │
│  │  + Management Signals (Finnhub): Insider buying, Buybacks >2%       │    │
│  │  + Momentum Check: Below 200MA (accumulation), Above 50MA (stable)  │    │
│  │                                                                     │    │
│  │  • Filter: Composite Score ≥ 60                                     │    │
│  │  • Output: ~50-100 qualified stocks                                 │    │
│  │  • Storage: MongoDB `pipeline.qualified_stocks`                     │    │
│  └─────────────────────────────────┬───────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  STAGE 3: CLASSIFICATION (On-Demand)                                │    │
│  │                                                                     │    │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐                              │    │
│  │  │   BUY   │  │  HOLD   │  │  WATCH  │                              │    │
│  │  │ MoS≥20% │  │ MoS±10% │  │  Other  │                              │    │
│  │  │Score≥70 │  │Score≥70 │  │Qualified│                              │    │
│  │  └─────────┘  └─────────┘  └─────────┘                              │    │
│  │                                                                     │    │
│  │  + Personal Risk Capacity Overlay                                   │    │
│  │  └── Filter by beta range, adjust MoS threshold, position sizing   │    │
│  │                                                                     │    │
│  │  • Computed on-demand from qualified_stocks + profile               │    │
│  │  • No storage needed                                                │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  LSTM-DCF INTEGRATION:                                                       │
│  ✓ Stage 1: Deep Value trigger uses LSTM fair value for MoS > 40%           │
│  ✓ Stage 2: VALUE pillar uses LSTM MoS, GROWTH pillar uses LSTM prediction  │
│  ✓ Stage 3: Classification thresholds based on LSTM-derived MoS             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Component Details

### Backend Components

| Component               | Location                                 | Responsibility                    |
| ----------------------- | ---------------------------------------- | --------------------------------- |
| **FastAPI App**         | `src/api/app.py`                         | Main application, CORS, routing   |
| **Screener Routes**     | `src/api/routes/screener.py`             | Stock screening endpoints         |
| **Risk Profile Routes** | `src/api/routes/risk_profile.py`         | Personal Risk Capacity API        |
| **Storage Routes**      | `src/api/routes/storage.py`              | MongoDB watchlist storage         |
| **StockScreener**       | `src/analysis/stock_screener.py`         | Core screening logic              |
| **RiskCapacityService** | `src/analysis/personal_risk_capacity.py` | Risk assessment & position sizing |
| **YFinanceFetcher**     | `src/data/fetchers.py`                   | Real-time data fetching           |
| **MongoClient**         | `src/data/mongo_client.py`               | MongoDB Atlas connection          |
| **LSTM-DCF Model**      | `src/models/deep_learning/`              | Fair value prediction             |
| **Agents**              | `src/agents/`                            | LangChain AI agents               |

### Data Storage

| Directory/Store   | Purpose                           | Persistence           |
| ----------------- | --------------------------------- | --------------------- |
| `data/raw/`       | API responses, financials         | Long-term             |
| `data/processed/` | Training datasets                 | Long-term             |
| `data/cache/`     | Screener cache                    | 6-hour TTL            |
| `models/`         | Trained model files               | Permanent             |
| **MongoDB Atlas** | Watchlists, strategies, education | Cloud (session-based) |
| **localStorage**  | Risk profiles                     | Browser (per-device)  |

---

## 🖥️ Frontend Architecture (Planned)

### Technology Stack

| Category             | Technology              | Purpose                   |
| -------------------- | ----------------------- | ------------------------- |
| **Framework**        | Next.js 14              | React framework with SSR  |
| **UI Library**       | TailwindCSS + shadcn/ui | Styling and components    |
| **Charts**           | Recharts or TradingView | Stock price visualization |
| **Data Fetching**    | React Query (TanStack)  | API caching and state     |
| **State Management** | Zustand                 | Global state (watchlist)  |
| **Forms**            | React Hook Form + Zod   | Validation                |

### Recommended Dependencies

```json
{
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@tanstack/react-query": "^5.0.0",
    "recharts": "^2.10.0",
    "tailwindcss": "^3.4.0",
    "@radix-ui/react-*": "latest",
    "zustand": "^4.4.0",
    "zod": "^3.22.0",
    "axios": "^1.6.0",
    "date-fns": "^3.0.0",
    "lucide-react": "^0.300.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/react": "^18.2.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0"
  }
}
```

### Frontend Component Structure

```
frontend/
├── app/                      # Next.js App Router
│   ├── page.tsx              # Dashboard (home)
│   ├── watchlist/
│   │   └── page.tsx          # Top undervalued stocks
│   ├── stock/
│   │   └── [ticker]/
│   │       └── page.tsx      # Stock detail page
│   ├── compare/
│   │   └── page.tsx          # Side-by-side comparison
│   └── sectors/
│       └── page.tsx          # Sector analysis
│
├── components/
│   ├── ui/                   # shadcn/ui components
│   ├── charts/
│   │   ├── PriceChart.tsx    # 1Y/5Y price chart
│   │   ├── TechnicalChart.tsx # MA50/MA200 overlay
│   │   └── SectorPieChart.tsx # Sector distribution
│   ├── cards/
│   │   ├── StockCard.tsx     # Watchlist item
│   │   ├── MetricCard.tsx    # P/E, P/B display
│   │   └── ScoreCard.tsx     # Valuation score
│   └── layout/
│       ├── Navbar.tsx
│       └── Footer.tsx
│
├── hooks/
│   ├── useWatchlist.ts       # GET /api/screener/watchlist
│   ├── useStock.ts           # GET /api/screener/stock/{ticker}
│   └── useCharts.ts          # GET /api/screener/charts/{ticker}
│
├── lib/
│   ├── api.ts                # Axios instance + endpoints
│   └── utils.ts              # Formatting helpers
│
└── types/
    └── api.ts                # TypeScript interfaces
```

---

## 🔐 Security Architecture

### API Security

| Layer                | Implementation                   |
| -------------------- | -------------------------------- |
| **CORS**             | Configured in FastAPI middleware |
| **Rate Limiting**    | Planned (FastAPI-limiter)        |
| **Input Validation** | Pydantic schemas                 |
| **Error Handling**   | HTTPException with codes         |

### Data Security

- Risk profiles stored client-side (localStorage) - no server storage
- Watchlists stored in MongoDB Atlas (session-based, no auth required)
- API keys stored in `.env` (not committed)
- External API calls use HTTPS

---

## 📊 3-Stage Stock Screening Pipeline

The core backend implements a **3-stage quality screening pipeline** for autonomous stock discovery:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     QUALITY STOCK SCREENING PIPELINE v3.0                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────┐      ┌─────────────────┐      ┌──────────────────┐        │
│  │   STAGE 0   │      │     STAGE 1     │      │     STAGE 2      │        │
│  │  Universe   │ ──▶  │ Attention Scan  │ ──▶  │ Pillar Scoring   │        │
│  │ Population  │      │ (5 Triggers)    │      │ (5 Pillars)      │        │
│  └─────────────┘      └─────────────────┘      └──────────────────┘        │
│      ~5800               ~20-25% trigger         ~40-50% qualify           │
│      stocks             (~1000-1400)             (~400-700)                │
│                               │                        │                    │
│                               ▼                        ▼                    │
│                        ┌──────────────────────────────────────┐            │
│                        │          STAGE 3: Curation           │            │
│                        │    Strong Buy │ Moderate │ Hold │ Watch          │
│                        │       ~15     │   ~15    │ ~30  │ ~10           │
│                        └──────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Stage Details

| Stage            | Script                         | Collection          | Frequency |
| ---------------- | ------------------------------ | ------------------- | --------- |
| **0. Universe**  | `populate_universe.py`         | `universe_stocks`   | Weekly    |
| **1. Attention** | `full_universe_scan.py`        | `attention_stocks`  | Weekly    |
| **2. Qualified** | `daily_qualified_update_v3.py` | `qualified_stocks`  | Daily     |
| **3. Curated**   | `stage3_curate_watchlist.py`   | `curated_watchlist` | On-demand |

### 5-Pillar Scoring System

| Pillar       | Weight | Key Metrics                                |
| ------------ | ------ | ------------------------------------------ |
| **Value**    | 20%    | Margin of Safety (LSTM-DCF), P/E vs Sector |
| **Quality**  | 20%    | FCF ROIC, ROE, Profit Margin               |
| **Growth**   | 20%    | Revenue Growth, LSTM Predicted Growth      |
| **Safety**   | 20%    | Beta, Volatility, Current Ratio            |
| **Momentum** | 20%    | Price vs 50/200 MA, RSI                    |

### API Endpoints

```
GET /api/pipeline/attention     # Stage 1 attention stocks
GET /api/pipeline/qualified     # Stage 2 qualified stocks
GET /api/pipeline/classified    # Stage 3 Buy/Hold/Watch lists
GET /api/pipeline/curated       # Stage 3 curated watchlist (web-ready)
GET /api/pipeline/stock/{ticker} # Single stock with pillar scores
```

> **Note**: Detailed pipeline specifications are archived in `docs/archive/PIPELINE_SPEC.md`

---

## 🏛️ Design Patterns & Best Practices

### Backend: Service-Oriented Architecture

The backend follows **Service Pattern** for maintainability and testability:

```
src/
├── api/routes/          # API Layer (thin controllers)
│   ├── screener.py      # Stock screener endpoints
│   ├── risk_profile.py  # Personal Risk Capacity endpoints
│   └── storage.py       # MongoDB storage endpoints
├── analysis/            # Business Logic Layer
│   ├── stock_screener.py
│   ├── valuation_analyzer.py
│   └── personal_risk_capacity.py  # Risk profile service
├── data/                # Data Fetcher Layer
│   ├── fetchers/
│   └── mongo_client.py  # MongoDB Atlas client
└── models/              # ML Model Layer
    └── deep_learning/
```

**Key Principles:**

- **Single Responsibility**: Each service handles one domain (portfolios, alerts, caching)
- **Dependency Injection**: Services receive dependencies, enabling mock testing
- **Loose Coupling**: Services don't depend on each other directly
- **Error Handling**: Services return None/empty on failure, never raise exceptions

### Frontend: Component-Based Architecture

The frontend follows **Atomic Design** with modular, reusable components:

```
frontend/src/
├── components/
│   ├── ui/              # Base components (shadcn/ui primitives)
│   │   ├── button.tsx   # Atomic: Button, Input, Badge
│   │   ├── card.tsx
│   │   └── tabs.tsx
│   ├── charts/          # Molecule: Chart wrappers
│   │   ├── PriceChart.tsx
│   │   └── SectorPieChart.tsx
│   ├── stocks/          # Organism: Stock-specific components
│   │   ├── StockCard.tsx
│   │   ├── WatchlistTable.tsx
│   │   └── MetricsPanel.tsx
│   └── layout/          # Template: Layout components
│       ├── Navbar.tsx
│       └── Sidebar.tsx
├── hooks/               # Custom hooks for data fetching
│   ├── useWatchlist.ts
│   └── useStock.ts
├── lib/                 # Utilities and API clients
│   ├── api.ts
│   └── utils.ts
└── app/                 # Pages (App Router)
    ├── page.tsx         # Dashboard
    └── stock/[ticker]/
```

**Key Principles:**

- **Composition over Inheritance**: Build complex UIs from simple components
- **Props-driven**: Components are configurable via props, not internal state
- **Co-location**: Keep related files together (component + styles + tests)
- **Type Safety**: Full TypeScript coverage with shared API types

### Why This Matters (Thesis Reference)

This architecture enables:

1. **Testability**: Mock services and components for unit testing
2. **Maintainability**: Changes to one layer don't affect others
3. **Scalability**: Add new features without refactoring existing code
4. **Team Collaboration**: Clear boundaries for parallel development
5. **Documentation**: Self-documenting code structure

---

## 📈 Scalability Considerations

### Current (Development)

- Single-process uvicorn server
- In-memory caching (6-hour expiry)
- Synchronous ML inference

### Production (Future)

| Component        | Scaling Strategy                         |
| ---------------- | ---------------------------------------- |
| **API**          | Multiple uvicorn workers, load balancer  |
| **Cache**        | Redis for distributed caching            |
| **ML Inference** | Model serving (TorchServe, Triton)       |
| **Database**     | MongoDB Atlas (already production-ready) |

---

_Next: [3. ML Pipeline](./03_ML_PIPELINE.md)_
